# Branch - users & auth

Authorization and authentication. This branch is responsible for managing user accounts, including login, password management and user roles. 
It also handles token generation and validation for secure access to the application.

It also adds Google Cloud Platform (GCP) deployment support and integration tests in GCP.
